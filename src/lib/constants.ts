/** Analysis step rotation interval (ms) */
export const ANALYSIS_STEP_INTERVAL_MS = 2500;

/** Milliseconds in one day */
export const MS_PER_DAY = 86_400_000;

/** Milliseconds in one minute */
export const MS_PER_MINUTE = 60_000;

/** Dashboard data refresh polling interval (ms) */
export const DASHBOARD_REFRESH_INTERVAL_MS = 30_000;

/** Language preference cookie max-age (seconds) — 1 year */
export const LANGUAGE_COOKIE_MAX_AGE = 31_536_000;

/** Float animation start delay (ms) */
export const FLOAT_ANIMATION_DELAY_MS = 800;

/** Touch long-press detection threshold (ms) */
export const TOUCH_LONG_PRESS_MS = 400;

/** Maximum events stored in local analytics queue */
export const MAX_LOCAL_EVENT_QUEUE = 50;

/** API route max duration (seconds) */
export const API_MAX_DURATION_SECONDS = 60;
